pipeline {
     agent any
         stages {
             stage('Build') {
                 steps {              

                    echo 'Application is in Building Phase'
                  	sh "mvn clean package"

                 }
            
			}
		}
}